You are fixing a critical bug in the trading engine where open
positions are not being closed when exit conditions are met,
causing stale signals to block new entries across all strategies.

CRITICAL RULE — read this before writing any code:
=======================================================
NEVER auto-close a signal unless ONE of these two conditions is met:

  1. The strategy's own exit logic fires (trailing stop hit,
     RSI cross, H1/EMA50 cross, time exit, etc.)

  2. A NEW signal for the same asset is generated by the SAME
     strategy in the OPPOSITE direction — the new signal
     explicitly replaces the old one.

DO NOT auto-close signals for any other reason:
  - NOT because a position record is missing
  - NOT because the signal is "old"
  - NOT because conditions have changed
  - NOT because another strategy has a conflicting signal
  - NOT on startup, NOT on a schedule, NOT via a cleanup job

The only things that may close a signal are:
  check_exits() in each strategy, and the new-signal replacement
  logic described in TASK 3 below.
=======================================================

Read ALL existing code before writing a single line.
Reference: multi_timeframe.py, trend_forex.py, trend_non_forex.py,
sp500_momentum.py, highest_lowest.py, strategy_engine.py,
database.py, admin.py, main.py

=============================================================
PROBLEM STATEMENT
=============================================================

EUR/USD shows an active BUY signal even though:
1. H1 close (1.14668) is below H4 EMA50 (1.15349) → LONG exit fired
2. SHORT conditions are now met
3. The SHORT signal cannot fire because BUY is still blocking it

Root cause: MultiTimeframeEMAStrategy has NO standalone check_exits()
method and the scheduler never calls one. Exit logic only runs inside
evaluate() per-asset when open_position_data is passed in. If evaluate()
is skipped for any reason, the exit never fires and the signal stays open
forever blocking new entries.

This same issue affects all strategies if their check_exits() is not
called from the scheduler — verified below:

STRATEGY          | Scheduler calls check_exits()? | Has standalone method?
------------------|-------------------------------|----------------------
sp500_momentum    | YES                           | YES
highest_lowest_fx | YES                           | YES
trend_forex       | YES                           | YES
trend_non_forex   | YES                           | YES
mtf_ema           | NO — MISSING                  | NO — MISSING

=============================================================
TASK 1 — DIAGNOSTIC ENDPOINTS (read-only, no changes to data)
=============================================================

Add two admin endpoints that show signal/position state without
modifying anything. Use these to understand what is wrong before
applying any fix.

A) Single asset diagnostic:

@router.get("/api/debug/open-signals/{asset}")
def debug_open_signals(request: Request, asset: str):
    guard = _admin_role_guard(request)
    if guard:
        return guard
    from trading_engine.models import Signal, OpenPosition

    asset_decoded = asset.replace("%2F", "/")

    with _get_session() as session:
        sigs = session.query(Signal).filter(
            Signal.asset == asset_decoded,
            Signal.status == "OPEN",
        ).order_by(Signal.id.desc()).all()

        pos = session.query(OpenPosition).filter(
            OpenPosition.asset == asset_decoded,
        ).all()

    return JSONResponse(content={
        "asset": asset_decoded,
        "open_signals": [
            {
                "id": s.id,
                "strategy_name": s.strategy_name,
                "direction": s.direction,
                "entry_price": s.entry_price,
                "stop_loss": s.stop_loss,
                "signal_timestamp": s.signal_timestamp,
                "created_at": str(s.created_at),
            }
            for s in sigs
        ],
        "open_positions": [
            {
                "id": p.id,
                "strategy_name": p.strategy_name,
                "direction": p.direction,
                "entry_price": p.entry_price,
                "atr_at_entry": p.atr_at_entry,
                "opened_at": str(p.opened_at),
            }
            for p in pos
        ],
        "signal_count": len(sigs),
        "position_count": len(pos),
        "cross_strategy": len(set(s.strategy_name for s in sigs)) > 1,
        "multi_direction": len(set(s.direction for s in sigs)) > 1,
    })

B) All assets diagnostic:

@router.get("/api/debug/all-open-signals")
def debug_all_open_signals(request: Request):
    guard = _admin_role_guard(request)
    if guard:
        return guard
    from trading_engine.models import Signal, OpenPosition
    from collections import defaultdict

    with _get_session() as session:
        all_sigs = session.query(Signal).filter(
            Signal.status == "OPEN"
        ).order_by(Signal.asset, Signal.id.desc()).all()

        all_pos = session.query(OpenPosition).order_by(
            OpenPosition.asset
        ).all()

    sigs_by_asset = defaultdict(list)
    for s in all_sigs:
        sigs_by_asset[s.asset].append({
            "id": s.id,
            "strategy_name": s.strategy_name,
            "direction": s.direction,
            "entry_price": s.entry_price,
            "created_at": str(s.created_at),
        })

    pos_by_asset = defaultdict(list)
    for p in all_pos:
        pos_by_asset[p.asset].append({
            "id": p.id,
            "strategy_name": p.strategy_name,
            "direction": p.direction,
            "entry_price": p.entry_price,
        })

    all_assets = sorted(
        set(list(sigs_by_asset.keys()) + list(pos_by_asset.keys()))
    )

    problems = []
    clean    = []

    for asset in all_assets:
        sigs = sigs_by_asset[asset]
        pos  = pos_by_asset[asset]
        issues = []

        if len(sigs) > 1:
            issues.append(f"multiple_open_signals ({len(sigs)})")
        if len(pos) > 1:
            issues.append(f"multiple_open_positions ({len(pos)})")
        if len(set(s["strategy_name"] for s in sigs)) > 1:
            issues.append("cross_strategy_signals")
        if len(set(s["direction"] for s in sigs)) > 1:
            issues.append("conflicting_directions")

        entry = {
            "asset": asset,
            "signals": sigs,
            "positions": pos,
            "issues": issues,
        }
        if issues:
            problems.append(entry)
        else:
            clean.append(asset)

    return JSONResponse(content={
        "total_assets": len(all_assets),
        "assets_with_problems": len(problems),
        "clean_assets": clean,
        "problems": problems,
        "totals": {
            "open_signals": len(all_sigs),
            "open_positions": len(all_pos),
        },
    })

NOTE: These endpoints are READ ONLY. They change nothing.
Use them to diagnose before and after applying fixes.

=============================================================
TASK 2 — multi_timeframe.py: add standalone check_exits() method
=============================================================

This is the primary fix. Add check_exits() as a method of
MultiTimeframeEMAStrategy. It uses the SAME exit logic already
in _check_h1_ema50_exit() — not new logic, just a standalone
caller that the scheduler can invoke directly.

ADD this method to the class AFTER evaluate():

def check_exits(self) -> list[dict]:
    """
    Standalone exit checker. Called by the scheduler after the
    per-asset evaluation loop. Ensures H1/EMA50 exits fire
    even if evaluate() was skipped for an asset.

    Exit rules (same as _check_h1_ema50_exit):
      LONG:  close when H1 close < H4 EMA50
      SHORT: close when H1 close > H4 EMA50

    IMPORTANT: only closes positions via the strategy's own exit
    logic. Never closes a position just because conditions changed
    or because the signal is old.
    """
    closed = []
    positions = get_all_open_positions(strategy_name=STRATEGY_NAME)

    if not positions:
        logger.info(
            "[MTF-EMA-EXIT] ====== check_exits | 0 open positions ======"
        )
        return closed

    logger.info(
        f"[MTF-EMA-EXIT] ====== check_exits | "
        f"{len(positions)} open position(s) ======"
    )

    for pos in positions:
        asset     = pos["asset"]
        pos_id    = pos["id"]
        direction = pos["direction"]

        logger.info(
            f"[MTF-EMA-EXIT] Position #{pos_id} | {asset} | "
            f"{direction} | entry={pos['entry_price']}"
        )

        # Fetch H1 candles for current price
        try:
            h1_candles = self.cache.get_candles(asset, TIMEFRAME_H1, 5)
        except Exception as e:
            logger.error(
                f"[MTF-EMA-EXIT] Position #{pos_id} | {asset} | "
                f"Exception fetching H1 candles: {e}"
            )
            continue

        if not h1_candles:
            logger.warning(
                f"[MTF-EMA-EXIT] Position #{pos_id} | {asset} | "
                f"No H1 candles — skipping"
            )
            continue

        # Fetch H4 candles for EMA50
        try:
            h4_candles = self.cache.get_candles(asset, TIMEFRAME_H4, 300)
        except Exception as e:
            logger.error(
                f"[MTF-EMA-EXIT] Position #{pos_id} | {asset} | "
                f"Exception fetching H4 candles: {e}"
            )
            continue

        if len(h4_candles) < EMA_50:
            logger.warning(
                f"[MTF-EMA-EXIT] Position #{pos_id} | {asset} | "
                f"Insufficient H4 candles: {len(h4_candles)}"
            )
            continue

        h4_closes       = [float(c["close"]) for c in h4_candles]
        h4_ema50_series = IndicatorEngine.ema(h4_closes, EMA_50)
        h4_ema50        = (
            h4_ema50_series[-1]
            if h4_ema50_series and h4_ema50_series[-1] is not None
            else None
        )

        if h4_ema50 is None:
            logger.warning(
                f"[MTF-EMA-EXIT] Position #{pos_id} | {asset} | "
                f"H4 EMA50 returned None — skipping"
            )
            continue

        h1_close = float(h1_candles[-1]["close"])

        logger.info(
            f"[MTF-EMA-EXIT] Position #{pos_id} | {asset} | {direction} | "
            f"H1_close={h1_close:.5f} | H4_EMA50={h4_ema50:.5f} | "
            f"LONG_exit(H1<EMA50)={h1_close < h4_ema50} | "
            f"SHORT_exit(H1>EMA50)={h1_close > h4_ema50}"
        )

        exit_triggered = False
        exit_reason    = ""

        if direction == "BUY" and h1_close < h4_ema50:
            breach = h4_ema50 - h1_close
            exit_reason = (
                f"H1/H4-EMA50 exit: H1 close {h1_close:.5f} < "
                f"H4 EMA50 {h4_ema50:.5f} (breach={breach:.5f})"
            )
            exit_triggered = True
            logger.info(
                f"[MTF-EMA-EXIT] Position #{pos_id} | EXIT LONG | "
                f"H1 {h1_close:.5f} < H4 EMA50 {h4_ema50:.5f}"
            )

        elif direction == "SELL" and h1_close > h4_ema50:
            breach = h1_close - h4_ema50
            exit_reason = (
                f"H1/H4-EMA50 exit: H1 close {h1_close:.5f} > "
                f"H4 EMA50 {h4_ema50:.5f} (breach={breach:.5f})"
            )
            exit_triggered = True
            logger.info(
                f"[MTF-EMA-EXIT] Position #{pos_id} | EXIT SHORT | "
                f"H1 {h1_close:.5f} > H4 EMA50 {h4_ema50:.5f}"
            )

        if exit_triggered:
            self._persist_exit(asset, exit_reason)
            closed.append({
                **pos,
                "exit_price": h1_close,
                "exit_reason": "h1_ema50_cross",
            })
        else:
            logger.info(
                f"[MTF-EMA-EXIT] Position #{pos_id} | {asset} | "
                f"Holding {direction} — exit condition not met"
            )

    logger.info(
        f"[MTF-EMA-EXIT] ====== check_exits complete | "
        f"{len(closed)} closed ======"
    )
    return closed

=============================================================
TASK 3 — all strategies: close old signal when new opposite
          signal fires for the same asset
=============================================================

RULE: when a strategy generates a new signal for an asset where
it already has an open signal in the OPPOSITE direction, the old
signal must be closed BEFORE the new one is inserted.

This is the only auto-close that is permitted — because it is
triggered by the strategy's own new signal, not by an external
cleanup process.

A) In database.py, add this function:

def close_opposite_signal_if_exists(
    strategy_name: str,
    asset: str,
    new_direction: str,
) -> bool:
    """
    If the strategy has an open signal for this asset in the
    OPPOSITE direction to new_direction, close it and delete
    the open position so the new signal can be inserted cleanly.

    Called by each strategy's evaluate() BEFORE insert_signal()
    when a new entry is about to fire.

    Returns True if an opposite signal was found and closed,
    False if no opposite signal existed (normal case).

    IMPORTANT: only closes signals from THIS strategy. Never
    touches signals owned by other strategies.
    """
    opposite = "SELL" if new_direction == "BUY" else "BUY"

    with _get_session() as session:
        try:
            sig = (
                session.query(Signal)
                .filter(
                    Signal.strategy_name == strategy_name,
                    Signal.asset == asset,
                    Signal.direction == opposite,
                    Signal.status == "OPEN",
                )
                .order_by(Signal.id.desc())
                .first()
            )

            if not sig:
                return False

            sig.status = "CLOSED"
            sig.exit_reason = (
                f"Closed by new {new_direction} signal for same asset "
                f"(direction flip: {opposite} → {new_direction})"
            )
            logger.info(
                f"[DB] close_opposite_signal_if_exists: closed signal #{sig.id} | "
                f"asset={asset} | strategy={strategy_name} | "
                f"old={opposite} → new={new_direction}"
            )

            # Also remove the open position so the new entry
            # can create a fresh one
            pos = (
                session.query(OpenPosition)
                .filter(
                    OpenPosition.asset == asset,
                    OpenPosition.strategy_name == strategy_name,
                )
                .first()
            )
            if pos:
                logger.info(
                    f"[DB] close_opposite_signal_if_exists: "
                    f"deleted position #{pos.id} | "
                    f"asset={asset} | strategy={strategy_name}"
                )
                session.delete(pos)

            session.commit()
            _invalidate_signal_cache()
            return True

        except Exception as e:
            session.rollback()
            logger.error(
                f"[DB] close_opposite_signal_if_exists failed: {e}"
            )
            return False

B) Import it in every strategy file:

    from trading_engine.database import (
        ...existing imports...,
        close_opposite_signal_if_exists,
    )

C) Call it in every strategy's evaluate() JUST BEFORE
   insert_signal() is called, for BOTH long and short entry blocks.

   For trend_forex.py — in LONG entry block:
     FIND (just before insert_signal(signal)):
         signal_id = insert_signal(signal)
     INSERT BEFORE:
         # Close opposite direction signal if this strategy has one open
         # (e.g. was SHORT, now going LONG — close the SHORT first)
         close_opposite_signal_if_exists(STRATEGY_NAME, asset, "BUY")

   Same pattern for SHORT entry block (new_direction="SELL"):
         close_opposite_signal_if_exists(STRATEGY_NAME, asset, "SELL")

   Apply the same pattern to ALL strategy files:
     trend_non_forex.py  — LONG block (BUY) and SHORT block (SELL)
     sp500_momentum.py   — LONG block only (BUY, LONG_ONLY strategy)
     highest_lowest.py   — both LONG (BUY) and SHORT (SELL) blocks
     multi_timeframe.py  — in _persist_entry() just before insert_signal()

   For multi_timeframe.py, in _persist_entry():
     FIND:
         signal_id = insert_signal(signal)
     INSERT BEFORE:
         close_opposite_signal_if_exists(
             STRATEGY_NAME, asset, direction
         )

=============================================================
TASK 4 — main.py: add check_exits() call to MTF EMA scheduler
=============================================================

In _scheduled_mtf_ema_evaluate(), AFTER the per-asset for loop
and BEFORE the status/finish_job_log lines, ADD:

    # ── Standalone exit check ──
    # Ensures H1/EMA50 exits fire even if evaluate() was skipped
    try:
        mtf_exits = strategy_engine.mtf_ema_strategy.check_exits()
        if mtf_exits:
            logger.info(
                f"[SCHEDULER] mtf_ema | "
                f"{len(mtf_exits)} exit(s) from check_exits()"
            )
            for ex in mtf_exits:
                logger.info(
                    f"[SCHEDULER] mtf_ema | EXIT: "
                    f"{ex.get('asset')} {ex.get('direction')} "
                    f"@ {ex.get('exit_price')} | "
                    f"reason={ex.get('exit_reason')}"
                )
        else:
            logger.info(
                "[SCHEDULER] mtf_ema | check_exits(): no exits"
            )
    except Exception as e:
        error_count += 1
        error_details.append(f"check_exits: {e}")
        logger.error(
            f"[SCHEDULER] mtf_ema | check_exits() exception: {e}",
            exc_info=True,
        )

=============================================================
TASK 5 — strategy_engine.py: add mtf_ema to
          check_exit_conditions()
=============================================================

FIND at end of check_exit_conditions():
    trend_non_forex_exits = self.trend_non_forex_strategy.check_exits()
    closed_signals.extend(trend_non_forex_exits)

    return closed_signals

REPLACE WITH:
    trend_non_forex_exits = self.trend_non_forex_strategy.check_exits()
    closed_signals.extend(trend_non_forex_exits)

    # MTF EMA was missing from this list
    try:
        mtf_exits = self.mtf_ema_strategy.check_exits()
        closed_signals.extend(mtf_exits)
        if mtf_exits:
            logger.info(
                f"[STRATEGY-ENGINE] mtf_ema check_exits: "
                f"{len(mtf_exits)} closed"
            )
    except Exception as e:
        logger.error(
            f"[STRATEGY-ENGINE] mtf_ema check_exits failed: {e}",
            exc_info=True,
        )

    return closed_signals

=============================================================
TASK 6 — database.py: improve has_any_open_signal_for_asset()
          logging
=============================================================

FIND has_any_open_signal_for_asset() and REPLACE the body with:

    with _get_session() as session:
        row = (
            session.query(
                Signal.id,
                Signal.strategy_name,
                Signal.direction,
            )
            .filter(
                Signal.asset == asset,
                Signal.status == "OPEN",
            )
            .first()
        )
        if row:
            logger.debug(
                f"[DB] has_any_open_signal_for_asset({asset}): "
                f"blocked by signal #{row[0]} | "
                f"strategy={row[1]} | direction={row[2]}"
            )
            return True
        return False

=============================================================
TASK 7 — admin.py: add "Run Cleanup" button for manual
          force-close via the existing force-close endpoint
=============================================================

The existing /admin/api/force-close-position endpoint already
handles manual closes correctly (admin-initiated, logged).

Add a diagnostic panel to the System Status tab that shows
signal integrity and links to the force-close workflow.

In the System Status tab HTML, ADD:

    <div class="settings-section" style="margin-top:20px;"
         data-testid="widget-signal-integrity">
        <h3>Signal Integrity</h3>
        <p style="color:#94a3b8;font-size:13px;margin-bottom:12px;">
            Shows assets where open signals and positions may be
            out of sync. Use the force-close endpoint to manually
            resolve specific positions after reviewing the diagnostic.
        </p>
        <button class="btn btn-secondary"
                onclick="loadSignalIntegrity()"
                id="btn-signal-integrity"
                data-testid="button-signal-integrity">
            Run Diagnostic
        </button>
        <div id="signal-integrity-result"
             style="margin-top:12px;"
             data-testid="text-signal-integrity-result"></div>
    </div>

Add to ADMIN_JS:

async function loadSignalIntegrity() {
    var btn = document.getElementById('btn-signal-integrity');
    var el  = document.getElementById('signal-integrity-result');
    btn.disabled = true;
    btn.textContent = 'Running...';
    el.innerHTML = '';
    try {
        var res  = await fetch(BASE + '/admin/api/debug/all-open-signals');
        var data = await res.json();
        var problems = data.problems || [];

        if (problems.length === 0) {
            el.innerHTML =
                '<div style="color:#22c55e;font-size:13px;">' +
                'All ' + data.total_assets + ' assets clean — ' +
                'no signal/position issues found.</div>';
        } else {
            var rows = problems.map(function(p) {
                var sigs = p.signals.map(function(s) {
                    return s.strategy_name + ' ' + s.direction +
                           ' #' + s.id;
                }).join(', ');
                return '<tr>' +
                    '<td style="font-weight:500;">' + p.asset + '</td>' +
                    '<td style="color:#f59e0b;font-size:12px;">' +
                    p.issues.join(', ') + '</td>' +
                    '<td style="font-size:12px;color:#94a3b8;">' +
                    sigs + '</td>' +
                    '</tr>';
            }).join('');

            el.innerHTML =
                '<div style="color:#f59e0b;font-size:13px;margin-bottom:8px;">' +
                problems.length + ' asset(s) with issues — ' +
                'use force-close to resolve individual positions</div>' +
                '<table class="data-table" style="font-size:12px;">' +
                '<thead><tr>' +
                '<th>Asset</th><th>Issue</th><th>Signals</th>' +
                '</tr></thead>' +
                '<tbody>' + rows + '</tbody>' +
                '</table>' +
                '<div style="margin-top:8px;font-size:12px;color:#64748b;">' +
                'To close a specific position: POST /admin/api/force-close-position' +
                '</div>';
        }
    } catch (e) {
        el.innerHTML =
            '<span style="color:#ef4444;">Error: ' + e.message + '</span>';
    }
    btn.disabled = false;
    btn.textContent = 'Run Diagnostic';
}

=============================================================
TASK 8 — admin.py: signal integrity card in analysis panel
=============================================================

In _get_signal_analysis_data(), ADD:

    all_positions = get_all_open_positions()
    pos_keys = set(
        (p["asset"], p.get("strategy_name"))
        for p in all_positions
    )
    sig_keys = set(
        (s["asset"], s.get("strategy_name"))
        for s in all_signals
        if s.get("status") == "OPEN"
    )

    # Assets with multiple open signals (any strategy)
    open_sig_assets = [
        s["asset"] for s in all_signals
        if s.get("status") == "OPEN"
    ]
    from collections import Counter
    asset_counts   = Counter(open_sig_assets)
    multi_sig_count = sum(1 for v in asset_counts.values() if v > 1)
    integrity_ok   = multi_sig_count == 0

Add to data dict:
    "multi_sig_assets": multi_sig_count,
    "integrity_ok": integrity_ok,

In _build_signal_analysis_html() summary_cards, ADD:

    <div class="stat-card">
        <div class="stat-label">Signal Integrity</div>
        <div class="stat-value"
             style="color:{'#22c55e' if data['integrity_ok'] else '#ef4444'};"
             data-testid="text-signal-integrity">
            {'OK' if data['integrity_ok'] else 'CHECK'}
        </div>
        <div class="stat-label" style="margin-top:4px;">
            Multi-signal assets: {data['multi_sig_assets']}
        </div>
    </div>

=============================================================
VERIFICATION CHECKLIST
=============================================================

1. Call GET /admin/api/debug/all-open-signals
   Note every asset in "problems" before any changes.

2. Deploy all changes and restart.

3. Wait for next MTF EMA scheduler run (:01 ET).
   Verify logs show:
   "[MTF-EMA-EXIT] ====== check_exits | N open position(s)"
   for EUR/USD: "[MTF-EMA-EXIT] Position #X | EXIT LONG |
   H1 1.14668 < H4 EMA50 1.15349"

4. Call GET /admin/api/debug/all-open-signals again.
   EUR/USD should no longer have a BUY open signal.

5. Wait for next :01 ET evaluation.
   EUR/USD SHORT conditions (C1 bearish + C2 rally-reject) should
   produce a new SELL signal. Verify in admin Signals tab.

6. Direction flip test:
   Insert a manual OPEN BUY for USO under strategy trend_non_forex.
   Then simulate SHORT conditions firing for USO.
   Verify: close_opposite_signal_if_exists() closes the BUY before
   the SELL is inserted. Only ONE signal remains open for USO.

7. NO auto-close test:
   Insert an old manual signal with an unknown strategy_name.
   Restart the server.
   Verify the signal is NOT closed — it is still OPEN.
   Only the force-close admin endpoint or strategy exit logic
   should ever close it.

8. Signal Integrity card in admin Signal Analysis tab shows "OK"
   after EUR/USD is resolved.

9. Verify all 5 strategies still function:
   - sp500_momentum: 30m entries and exits unaffected
   - trend_forex: 17:01 ET entries and exits unaffected
   - trend_non_forex: 16:01 ET entries and exits unaffected
   - highest_lowest_fx: 09:00/10:00 ET unaffected
   - mtf_ema: hourly evaluate() + new check_exits() both run

=============================================================
CONSTRAINTS
=============================================================

NEVER auto-close a signal except:
  1. Strategy's own exit logic fires (check_exits, trailing stop,
     RSI cross, H1/EMA50, time exit)
  2. close_opposite_signal_if_exists() — only when the SAME
     strategy generates a new signal in the opposite direction

DO NOT add any scheduled cleanup job that closes signals.
DO NOT close signals in init_db() or on startup.
DO NOT close signals based on age, price, or condition checks
  outside of the strategy's own exit methods.

Other constraints:
- Do NOT change exit condition logic in _check_h1_ema50_exit()
- Do NOT change TIMEFRAME_H1, TIMEFRAME_H4, or EMA_50 constants
- Do NOT change trailing stop logic in trend_forex or
  trend_non_forex — dynamic ATR model is correct and unchanged
- All DB operations use _get_session() / SessionFactory
- close_opposite_signal_if_exists() only touches signals owned
  by the calling strategy — never another strategy's signals
- Preserve ALL existing log prefixes — only add new lines
- Python type hints on all new and modified functions
- Import close_opposite_signal_if_exists in every strategy file
- The diagnostic endpoints require admin auth via _admin_role_guard()