In trading_engine/api_v1.py, add exit_rules to the /api/v1/strategies endpoint response. Each strategy object should include a human-readable exit rule string.
1. Add an EXIT_RULES_PUBLIC dict near the top of api_v1.py just below the existing _EXIT_INSTRUCTIONS dict:
pythonEXIT_RULES_PUBLIC: dict[str, str] = {
    "mtf_ema": "Exit when H1 close crosses below H1 EMA20 (LONG) or above H1 EMA20 (SHORT), or when the 2× ATR trailing stop is breached.",
    "trend_forex": "Dynamic 3× ATR trailing stop ratcheted daily at 5:01 PM ET. Stop only moves in the trade's favour — never back.",
    "trend_non_forex": "Dynamic 3× ATR trailing stop ratcheted daily at 4:01 PM ET. Stop only moves in the trade's favour — never back.",
    "sp500_momentum": "Exit when RSI(20) drops below 70 during ARCA session (09:30–15:30 ET). No trailing stop — RSI threshold is the sole exit trigger.",
    "highest_lowest_fx": "Exit after 6 hours from entry (time exit), or immediately if the 2× ATR(100) stop loss is hit — whichever comes first.",
    "stocks_algo1": "Exit at monthly rebalance if the symbol drops out of the top 20 momentum ranking, or immediately if price falls 8% below entry.",
    "stocks_algo2": "Exit when price falls 4% below entry (stop loss), or automatically after 5 trading days — whichever comes first.",
}
2. Update list_strategies() to include exit_rules in each strategy dict
Find the list_strategies() function. It currently builds strategy_stats like this:
pythonstrategy_stats[name] = {
    "name": name,
    "label": STRATEGY_LABELS.get(name, name),
    "total": 0,
    "open": 0,
    "closed": 0,
}
Replace with:
pythonstrategy_stats[name] = {
    "name": name,
    "label": STRATEGY_LABELS.get(name, name),
    "total": 0,
    "open": 0,
    "closed": 0,
    "exit_rules": EXIT_RULES_PUBLIC.get(name),
}
3. Update the StrategySummary Pydantic model
Find:
pythonclass StrategySummary(BaseModel):
    name: str
    label: str
    total: int
    open: int
    closed: int
Replace with:
pythonclass StrategySummary(BaseModel):
    name: str
    label: str
    total: int
    open: int
    closed: int
    exit_rules: Optional[str] = None
4. Expected response shape after the fix:
json{
  "strategies": [
    {
      "name": "mtf_ema",
      "label": "MTF EMA",
      "total": 7,
      "open": 2,
      "closed": 5,
      "exit_rules": "Exit when H1 close crosses below H1 EMA20 (LONG) or above H1 EMA20 (SHORT), or when the 2× ATR trailing stop is breached."
    },
    {
      "name": "highest_lowest_fx",
      "label": "Highest/Lowest FX",
      "total": 2,
      "open": 0,
      "closed": 2,
      "exit_rules": "Exit after 6 hours from entry (time exit), or immediately if the 2× ATR(100) stop loss is hit — whichever comes first."
    },
    {
      "name": "stocks_algo2",
      "label": "Stocks Algo 2 — Mean Reversion",
      "total": 2,
      "open": 2,
      "closed": 0,
      "exit_rules": "Exit when price falls 4% below entry (stop loss), or automatically after 5 trading days — whichever comes first."
    }
  ],
  "count": 3
}
5. Do NOT change:

The _EXIT_INSTRUCTIONS dict or _get_exit_instruction() function
The STRATEGY_LABELS dict
Any other endpoint or response shape
The cache decorator on list_strategies()
Any strategy logic or database functions